import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles({
    btnenvoyer: {
        borderRadius: '20px',
        width: '180px',
        marginTop: '25px'
    },
    qtewidth: {
        width: '65px'
    },
    spanweight: {
      fontWeight: 600
  }
  });